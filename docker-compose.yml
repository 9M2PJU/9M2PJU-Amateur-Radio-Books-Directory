services:
    nginx:
        image: nginx:alpine
        container_name: books-directory
        restart: unless-stopped
        ports:
            - '9082:80'
        volumes:
            - ./public:/usr/share/nginx/html:ro
            - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
            - /media/Apps/Amatur/books:/books:ro
